import React from 'react'
import {db} from './config'
import {itemId} from '../components/ItemDetailContainer/ItemDetailContainer'

export const detail = () => {
    return new Promise((resolve, reject) => {
        const 
    }) 
}
